# Telegram Bot Framework
aiogram==3.0.0b7

# HTTP Client (обновленная версия для Python 3.14)
aiohttp>=3.10.0

# Database
aiosqlite==0.18.0

# Environment Variables
python-dotenv==1.0.0

# Scheduling (используется в проекте, но можно удалить если не нужно)
apscheduler==3.10.1

# WebSocket для арбитражного бота
websockets==12.0

# Опционально: uvloop для лучшей производительности
uvloop==0.19.0; sys_platform != 'win32'

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
